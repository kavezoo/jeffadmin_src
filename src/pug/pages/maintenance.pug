extends ../layouts/_error

block variables
  - var pageMeta = { "title": "Under maintenance", "description": "Scheduled maintenance.", "noindex": true }
  - var bodyClass = 'app error-page'

block content
  include ../partials/content/maintenance.html
