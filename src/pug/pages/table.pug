extends ../layouts/_default

block variables
  - var pageMeta = { title: 'Data tables', description: 'Responsive data tables with horizontal scroll affordances and striped/hover variants.' }
  - var activePage = 'table.html'

block content
  include ../partials/content/table.html
