extends ../layouts/_error

block variables
  - var pageMeta = { "title": "Server error", "description": "Internal server error.", "noindex": true }
  - var bodyClass = 'app error-page'

block content
  include ../partials/content/500.html
